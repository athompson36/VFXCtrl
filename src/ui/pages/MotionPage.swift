import SwiftUI

struct MotionPage: View {
    var body: some View { PageGrid(keys: ["motion.position", "motion.amount", "motion.source", "lfo1.rate", "lfo1.depth", "lfo2.rate", "modwheel.depth", "aftertouch.depth"]) }
}
