import SwiftUI

struct FilterPage: View {
    var body: some View { PageGrid(keys: ["filter.cutoff", "filter.resonance", "filter.env", "filter.velocity", "filter.keytrack", "filter.mode", "filter.source", "filter.alt"]) }
}
