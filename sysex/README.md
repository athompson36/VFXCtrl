# SysEx captures (`sysex/`)

This folder is for **real VFX-SD** program dumps and your test captures.

## Phase 0 checklist ([`TODO.md`](../TODO.md))

1. Save at least one **current program** dump as `.syx` (e.g. `baseline_current_program.syx`).
2. Follow naming hints in [`notes.md`](./notes.md).
3. Record **synth OS**, MIDI interface, and quirks in `notes.md`.

The app does not require files here to build; parser tests use synthetic data in unit tests.
